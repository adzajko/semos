const concatenateStrings = (string1, string2) => {
  return string1 + string2;
};

/**
 * module.exports can be used to export ANYTHING from a file, including functions, objects, arrays or plain variables.
 */
module.exports = concatenateStrings;
